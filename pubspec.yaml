name: ios_search
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter   # Küçük harf
  http: ^1.1.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  path_provider: ^2.0.14
  fluttertoast: ^8.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
